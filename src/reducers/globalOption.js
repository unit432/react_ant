const initialState = {
  host: '127.0.0.1',
  port: '6800'
}

const globalOption = (state = initialState, action) => {
  switch(action.type){
    default:
      return state
  }
}

export default globalOption
